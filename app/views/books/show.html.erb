<%= render "field_titles" %>
<%= render "books" , book: @book %>
</br></br>
  <h4>
    <div class="col-lg-offset-4 col-lg-2">Recipes</div>
    <div class="col-lg-6">Directions </div>
  </h4>
  <% @recipes.each_with_index do |recipe, i| %>
    <div class="col-lg-offset-4 col-lg-2"><%= @recipes[i][:title] %></div>
    <div class="col-lg-6"><%= @recipes[i][:directions]%> </div>
  <% end %>
</br></br>
<div class="col-lg-offset-5 col-lg-2"><%= link_to "back to all books", books_path %></div>